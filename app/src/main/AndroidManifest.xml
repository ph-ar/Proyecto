<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.jrmg.proyectoaphm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DesvenActivity"></activity>
        <activity android:name=".ImportanciaActivity" />
        <activity android:name=".AcercadeActivity" />
        <activity android:name=".CuartapartActivity" />
        <activity android:name=".TercerapartActivity" />
        <activity android:name=".SegundapartActivity" />
        <activity android:name=".PrimerapartActivity" />
        <activity android:name=".IntroduccionActivity" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>